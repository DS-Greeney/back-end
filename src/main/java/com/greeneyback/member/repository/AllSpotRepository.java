package com.greeneyback.member.repository;

import com.greeneyback.member.entity.AllSpotEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AllSpotRepository extends JpaRepository<AllSpotEntity, Integer> {
}
